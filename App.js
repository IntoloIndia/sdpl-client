import React from 'react';
import {View, Text} from 'react-native';
import FormInput from './src/FormInput';

const App = () => {
  return (
    <View
      style={{
        backgroundColor: 'red',
      }}>
      <FormInput />
    </View>
  );
};

export default App;
